class marker {
  //modal class for Person object
  String id, title, kabupaten;
  double lat, lang;
  marker(
      {required this.id,
      required this.lat,
      required this.lang,
      required this.title,
      required this.kabupaten});
}
